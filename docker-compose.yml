services:
  web:
    build: .
    container_name: sana_sana
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
      - FLASK_DEBUG=0
    volumes:
      - ./app/static/uploads:/app/app/static/uploads
    restart: unless-stopped
